# Codebase-PROMPT-Engine
Context-aware prompt builder that scans your local codebase, chunks it semantically via AST parsing, ranks chunks with TF-IDF cosine similarity, resolves cross-file dependencies, and outputs a structured prompt ready to paste into Claude or ChatGPT — fully offline, zero LLM API calls.
